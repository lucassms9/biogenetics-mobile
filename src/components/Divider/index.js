import React from 'react';
import { View } from 'react-native';

const Divider = () => (
  <View>
    <View
      style={{
        backgroundColor: '#004ba7',
        height: 1.5,
        width: '100%',
        marginTop: 5,
      }}
    />
  </View>
);

export default Divider;
